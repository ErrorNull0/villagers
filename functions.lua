function villagers.getItemsToSell(self)
	--region type: dictates natural resources like sand, snow blocks, dirt
	--building type: dictates specific main items: fences, slabs, cotton, wheet, anvil, sword, pick
	--if building type empty, deco, allmende, park, etc, then sell nothing.. or some based on region type
	--if age is young sell only one food item.
	
	
	
end

function villagers.getItemsDesired(self)
	
end